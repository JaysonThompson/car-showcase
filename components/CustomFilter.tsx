import { CustomFilterProps } from "@/types";

const CustomFilter = ({
	title,
	containerStyles,
	handleClick,
}: CustomFilterProps) => {
	return <div>CustomFilter</div>;
};

export default CustomFilter;
